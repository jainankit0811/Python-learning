# creating a tuples using ()
t = (1, 2, 4, 6)
# t1 = () # Empty tuple
# t1 = (1) # Wrong way to decleare a tuple with single element
t1 = (1,)  # Tuple with single element
print(t1)
# printing the elements of a tuple
# print(t[0])

# t[0] = 34 # cannot update the value of a tuple
